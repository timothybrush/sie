{
  "name": "stripe-link-fraud",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "docker compose up -d && tsx web/server.ts",
    "ui": "tsx web/server.ts",
    "index": "tsx src/index-build.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@superlinked/sie-sdk": "^0.3.1",
    "stripe": "^17.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.21.0",
    "typescript": "^5.7.2"
  }
}
